# Use a project-local pnpm store so all installs (incl. `expo install`)
# resolve against the same store the workspace node_modules is linked to.
# This avoids ERR_PNPM_UNEXPECTED_STORE when the global store differs.
store-dir=.pnpm-store
